# 次の実装作業への引き継ぎ

最初にREADMEの7ボード、SPEC、tokens.jsonを見る。画像だけで通信・権限・空状態を推測しない。このコミットは設計資料のみで、Swiftの修正を含まない。

1. **既存の挙動を直す。** Features/Gallery/GalleryView.swift、HomeFeedCard.swiftで重複したフィード選択を一列に集約する。空ストーリー枠を非表示にする。カルーセルの操作は表示中の写真IDに固定し、通信中のスワイプで対象を変えない。0件と通信失敗を分ける。既存の閲覧・投稿権限を維持する。
2. **画像01〜06を実装。** 共通の余白・文字・面をtokensへ集約。ホーム→検索→詳細→マイページ→投稿の順に置換する。写真詳細は元の構図を見せる。Photos/Upload/Profile各機能のルート名は実装時の現行treeで照合する。
3. **画像09〜16の既存機能を整える。** 保存・スポット・旅の一冊・通知・設定・ストーリーに共通部品を適用。TripsView/TripBookViewの既存データを維持。マップはSPECに従いMapKitを継承。
4. **画像17〜19を段階導入。** WEB_FEATURESの準備条件を満たすまで機能フラグで隠す。WishlistStoreをAPIと接続し、ローカル保存を安全に移行。TripPlan専用モデルとサービスを追加。ガイドは出典付き公開データを取得できるまで未登録表示を使う。

各段階で小画面・Dynamic Type・VoiceOver・キーボード・通信失敗をSPEC末尾の受け入れ条件で検証する。Xcodeビルドと既存テストはmacOS環境で実行する。ブラウザモックの見栄え確認をiOSテストの代わりにしない。

サイトのfavicon/PWAアイコン/ロゴ、iOS AppIcon/AppLogo.swiftは変更対象外。仕様にない装飾や新しいブランドマークを追加しない。
