# 検証記録

2026-09-25 / Windows / Chrome 153.0.8010.36 / Playwright Core 1.63.0

- 19枚の画面・状態PNG（780×1688）と7枚のボード（2880×2330）を同じHTML/CSSから生成。
- 全画面の画像decode成功、JavaScript例外0件。保存ボタンのaria-pressed切り替え、ボード切り替えを確認。
- 7ボードを目視確認。写真の表示、文字、操作バー、スクロール領域を確認。ブラウザ描画で一度発生した写真の欠落はGPU無効化と描画フレーム待機を追加して解消。
- 既存サイトアイコンのGit blob SHAは `6f36a54375ca223a4330ee90240762816459c560`。資料のコピーも同一。再着色・描き直しなし。
- READMEから参照するローカル文書・画像の存在とGitの空白エラーを確認。

これは設計資料の検証。SwiftUI実装、実機、Dynamic Type、VoiceOver、ライブAPI、写真公開権限の動作は未検証。実装時はSPECの受け入れ条件を別途満たすこと。
